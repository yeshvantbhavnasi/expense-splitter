[phases.setup]
nixPkgs = ["python311", "python311.pkgs.pip"]

[phases.install]
cmds = [
    "cd backend",
    "pip install -r requirements.txt"
]

[start]
cmd = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
